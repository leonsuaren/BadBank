import { render } from '@testing-library/react';
import { Deposit } from './Deposit';

describe('user make a deposit', () => {
  test('renders without error', () => {
    // const { getByText, getByPlaceholderText } = render(<Deposit />);
    // const depositInput = getByPlaceholderText('Enter Deposit Amount');
    // expect(depositInput.length).toBe(1);
    });
});